<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="Products"
      focusComponent="productsDataGrid">
    <data>
        <collection id="productsDc"
                    class="com.digtp.scm.entity.Product">
            <fetchPlan extends="_base"/>
            <loader id="productsDl" readOnly="true">
                <query>
                    <![CDATA[select e from scm_Product e order by e.name]]>
                </query>
            </loader>
        </collection>
    </data>
    <actions>
        <action id="selectAction" type="lookup_select"/>
        <action id="discardAction" type="lookup_discard"/>
    </actions>
    <facets>
        <dataLoadCoordinator auto="true"/>
    </facets>
    <layout>
        <dataGrid id="productsDataGrid"
                  width="100%"
                  minHeight="20em"
                  dataContainer="productsDc">
            <columns>
                <column property="name" header="Name"/>
            </columns>
        </dataGrid>
        <hbox id="lookupActions" visible="false">
            <button id="selectButton" action="selectAction"/>
            <button id="discardButton" action="discardAction"/>
        </hbox>
    </layout>
</view>

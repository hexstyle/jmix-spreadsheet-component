<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<view xmlns="http://jmix.io/schema/flowui/view"
      title="Tracks"
      focusComponent="tracksDataGrid">
    <data>
        <collection id="tracksDc"
                    class="com.example.scm.entity.Track">
            <fetchPlan extends="_base"/>
            <loader id="tracksDl" readOnly="true">
                <query>
                    <![CDATA[select e from scm_Track e order by e.name]]>
                </query>
            </loader>
        </collection>
    </data>
    <actions>
        <action id="selectAction" type="lookup_select"/>
        <action id="discardAction" type="lookup_discard"/>
    </actions>
    <facets>
        <dataLoadCoordinator auto="true"/>
    </facets>
    <layout>
        <dataGrid id="tracksDataGrid"
                  width="100%"
                  minHeight="20em"
                  dataContainer="tracksDc">
            <columns>
                <column property="name" header="Name"/>
                <column property="trackType" header="Type"/>
            </columns>
        </dataGrid>
        <hbox id="lookupActions" visible="false">
            <button id="selectButton" action="selectAction"/>
            <button id="discardButton" action="discardAction"/>
        </hbox>
    </layout>
</view>

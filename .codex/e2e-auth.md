# Codex E2E auth note

For browser/e2e checks in this repository use:

- **username:** `admin`
- **password:** `1`

## Important

`VAADIN_OFFLINE_KEY` is a **server license** key and works only with **production builds**.
When running e2e flows that render SpreadsheetComponent, run the app/tests in production mode (not Vaadin development mode).
